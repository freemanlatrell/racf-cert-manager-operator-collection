# Pinned Python development tooling for the racf-cert-manager operator collection.
# ansible-lint brings its own compatible ansible-core, which provides
# ansible / ansible-playbook / ansible-galaxy for building and syntax checks.
ansible-lint==26.8.0
yamllint==1.38.0
